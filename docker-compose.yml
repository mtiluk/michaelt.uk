services:
  web:
    build:
      context: .
      args:
        NEXT_PUBLIC_SITE_URL: ${NEXT_PUBLIC_SITE_URL}
    image: michaelt:latest
    container_name: michaelt-web
    restart: unless-stopped
    env_file: .env
    ports:
      - "3001:3000"
    networks:
      - web

networks:
  web:
